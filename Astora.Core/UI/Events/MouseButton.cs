namespace Astora.Core.UI.Events;

/// <summary>
/// Identifies which mouse button generated the event (maps from XNA ButtonState usage).
/// </summary>
public enum MouseButton
{
    Left,
    Right,
    Middle,
    XButton1,
    XButton2
}
